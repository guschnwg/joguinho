diff --git a/node_modules/matter-js/build/matter.js b/node_modules/matter-js/build/matter.js
index b36c3f5..b9c134e 100644
--- a/node_modules/matter-js/build/matter.js
+++ b/node_modules/matter-js/build/matter.js
@@ -8984,6 +8984,7 @@ var Mouse = _dereq_('../core/Mouse');
 
                     c.translate(part.position.x, part.position.y);
                     c.rotate(part.angle);
+                    part.render.sprite.flip && c.scale(-1, 1);
 
                     c.drawImage(
                         texture,
@@ -8994,6 +8995,7 @@ var Mouse = _dereq_('../core/Mouse');
                     );
 
                     // revert translation, hopefully faster than save / restore
+                    part.render.sprite.flip && c.scale(-1, 1);
                     c.rotate(-part.angle);
                     c.translate(-part.position.x, -part.position.y);
                 } else {
